import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  converterContainer: {
    width: '100%',
    height: '75%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  elems: {
    width: '100%',
    height: '100%',
    display: 'flex'
  }
})

export {styles};